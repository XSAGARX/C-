//Write a C program to calculate area of a reactangle:
// so the formula for the area of the rectangle is A=w*L 

#include <stdio.h>
int main(){
    int length=5, breadth=6;
    int area = length*breadth;
    printf("The area of this reactangle is %d", area);
    return 0;
    
}